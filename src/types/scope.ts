export interface ScopeItem {
  id: string;
  system: string;
  component: string;
  element: string;
  description: string;
}

export interface ImpactItem {
  id: string;
  system: string;
  component: string;
  element: string;
  description: string;
}

export interface ChecklistItem {
  id: string;
  element: string;
  verification: string;
}
